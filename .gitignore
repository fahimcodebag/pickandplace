# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Training artifacts
# Model weights ARE tracked (~9 MB); only the replay buffers are excluded --
# they are 70-702 MB each, regenerable, and exceed GitHub's 100 MB file limit.
checkpoints/**/replay_buffer.npz
logs/

# OS / editor metadata
*.Zone.Identifier
.DS_Store

*Zone.Identifier
